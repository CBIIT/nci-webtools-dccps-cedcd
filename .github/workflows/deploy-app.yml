name: Deploy CEDCD
permissions:
  id-token: write
  contents: read

on:
  workflow_dispatch:
    inputs:
      tier:
        description: "Deploy environment"
        required: true
        default: "dev"
        type: choice
        options:
          - dev
          - qa
          - stage
          - prod
      disable_backend_cache:
        description: "Disable Docker cache for backend image build"
        required: true
        default: false
        type: boolean

jobs:
  deploy-fargate-app:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    environment: ${{ inputs.tier }}
    env:
      APP: cedcd
      TZ: America/New_York
      AWS_REGION: us-east-1
      TASK_DEFINITION_TEMPLATE_PATH: .github/aws
      FRONTEND_CONTAINER_PORT: 80
      BACKEND_CONTAINER_PORT: 8221
      TIER: ${{ inputs.tier }}
      CICD_ROLE_ARN: arn:aws:iam::${{ vars.AWS_ACCOUNT_ID }}:role/power-user-github-actions-cicd
      ECS_DATA_IMPORT_CPU_UNITS: "1024"
      ECS_DATA_IMPORT_MEMORY_UNITS: "2048"

    steps:
      - uses: "actions/checkout@v6.0.2"

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6.0.0
        with:
          role-to-assume: ${{ env.CICD_ROLE_ARN }}
          role-session-name: ${{ env.TIER }}-${{ env.APP }}-deploy-${{ github.ref_name }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Set dynamic environment variables
        run: |
          DEPLOY_DATE=$(date +"%Y-%m-%d")
          TIMESTAMP=$(date +"%Y%m%d%H%M%S")
          REPO=${{ vars.AWS_ACCOUNT_ID }}.dkr.ecr.$AWS_REGION.amazonaws.com/$APP

          echo "DEPLOY_DATE=$DEPLOY_DATE" >> $GITHUB_ENV
          echo "IMAGE_REPOSITORY=$REPO" >> $GITHUB_ENV
          echo "FRONTEND_IMAGE=$REPO:frontend-${{ github.ref_name }}-$TIMESTAMP" >> $GITHUB_ENV
          echo "BACKEND_IMAGE=$REPO:backend-${{ github.ref_name }}-$TIMESTAMP" >> $GITHUB_ENV
          echo "FRONTEND_IMAGE_LATEST=$REPO:frontend-${{ github.ref_name }}-latest" >> $GITHUB_ENV
          echo "BACKEND_IMAGE_LATEST=$REPO:backend-${{ github.ref_name }}-latest" >> $GITHUB_ENV
          echo "DATA_IMPORT_IMAGE=$REPO:data-import-${{ github.ref_name }}-$TIMESTAMP" >> $GITHUB_ENV
          echo "DATA_IMPORT_IMAGE_LATEST=$REPO:data-import-${{ github.ref_name }}-latest" >> $GITHUB_ENV
          echo "ENVIRONMENT_TIER=${TIER^^}" >> $GITHUB_ENV

          if [[ "$TIER" == "dev" ]]; then
              echo "LOG_LEVEL=debug" >> $GITHUB_ENV
          else
              echo "LOG_LEVEL=info" >> $GITHUB_ENV
          fi

      - name: Retrieve SSM parameters
        run: |
          PARAMETER_PATH="/analysistools/${TIER}/${APP}"

          # Define parameters to retrieve (SSM name = ENV_VAR_NAME)
          declare -A PARAMS=(
            ["ecs_cluster"]="ECS_CLUSTER"
            ["ecs_web_task"]="ECS_WEB_TASK"
            ["ecs_web_task_cpu_units"]="ECS_WEB_TASK_CPU_UNITS"
            ["ecs_web_task_memory_units"]="ECS_WEB_TASK_MEMORY_UNITS"
            ["ecs_web_service"]="ECS_WEB_SERVICE"
            ["data_import_task"]="ECS_DATA_IMPORT_TASK"
            ["role_arn"]="ROLE_ARN"
            ["efs_filesystem_id"]="EFS_FILESYSTEM_ID"
            ["efs_access_point_id"]="EFS_ACCESS_POINT_ID"
          )

          # Retrieve each parameter and export to environment
          for ssm_name in "${!PARAMS[@]}"; do
            env_var="${PARAMS[$ssm_name]}"
            value=$(aws ssm get-parameter \
              --name "${PARAMETER_PATH}/${ssm_name}" \
              --with-decryption \
              --query "Parameter.Value" \
              --output text)
            echo "${env_var}=${value}" >> $GITHUB_ENV
            echo "Retrieved ${ssm_name} -> ${env_var}"
          done

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2.1.0
        with:
          mask-password: true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4.0.0

      - name: Create and use a new builder instance
        run: |
          docker buildx create --name mybuilder --use

      - name: Build backend image ${{ env.BACKEND_IMAGE }}
        uses: docker/build-push-action@v7.0.0
        with:
          context: .
          file: docker/backend.dockerfile
          pull: true
          no-cache: ${{ inputs.disable_backend_cache }}
          push: true
          tags: |
            ${{ env.BACKEND_IMAGE }}
            ${{ env.BACKEND_IMAGE_LATEST }}
          cache-from: type=registry,ref=${{ env.BACKEND_IMAGE_LATEST }}
          cache-to: type=inline

      - name: Build frontend image ${{ env.FRONTEND_IMAGE }}
        uses: docker/build-push-action@v7.0.0
        with:
          context: .
          file: docker/frontend.dockerfile
          pull: true
          push: true
          tags: |
            ${{ env.FRONTEND_IMAGE }}
            ${{ env.FRONTEND_IMAGE_LATEST }}
          build-args: |
            REACT_APP_VERSION=${{ github.ref_name }}
          cache-from: type=registry,ref=${{ env.FRONTEND_IMAGE_LATEST }}
          cache-to: type=inline

      - name: Build data import image ${{ env.DATA_IMPORT_IMAGE }}
        uses: docker/build-push-action@v7.0.0
        with:
          context: .
          file: docker/data-import.dockerfile
          pull: true
          push: true
          tags: |
            ${{ env.DATA_IMPORT_IMAGE }}
            ${{ env.DATA_IMPORT_IMAGE_LATEST }}
          cache-from: type=registry,ref=${{ env.DATA_IMPORT_IMAGE_LATEST }}
          cache-to: type=inline

      - name: Substitute web task definition variables
        id: substitute-web-task-definition
        run: |
          echo "Substituting web task definition variables"
          envsubst < $TASK_DEFINITION_TEMPLATE_PATH/web.yml > web.yml

      - name: Substitute data import task definition variables
        id: substitute-data-import-task-definition
        run: |
          echo "Substituting data import task definition variables"
          envsubst < $TASK_DEFINITION_TEMPLATE_PATH/data-import.yml > data-import.yml

      - name: Print rendered web task definition
        run: |
          echo "Rendered web Task Definition:"
          cat web.yml

      - name: Register task definitions
        run: |
          aws ecs register-task-definition --cli-input-yaml file://web.yml
          aws ecs register-task-definition --cli-input-yaml file://data-import.yml

      - name: Update web service
        run: |
          aws ecs update-service \
              --cluster ${{ env.ECS_CLUSTER }} \
              --service ${{ env.ECS_WEB_SERVICE }} \
              --task-definition ${{ env.ECS_WEB_TASK }} \
              --desired-count 1 \
              --propagate-tags TASK_DEFINITION \
              --force-new-deployment

      - name: Remove old task definitions
        run: |
          for family in "$ECS_WEB_TASK" "$ECS_DATA_IMPORT_TASK"; do
              echo "Pruning old revisions for family: $family"
              aws ecs list-task-definitions --family-prefix "$family" --sort DESC --query 'taskDefinitionArns[3:]' --output text \
                  | xargs -n1 -r aws ecs deregister-task-definition --task-definition
          done

      - name: Deployment summary
        run: |
          echo "## Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Tier:** ${{ env.TIER }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Branch:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
